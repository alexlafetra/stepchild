graphical bugs/issues
-------------------------------------------
more obvious loop menu: loop info on Right side and prompt for (sel to edit loop)
files fly out too fast in file menu

autotracks don't fit in previsualizer box on the bottom
also, channel number is off by one in the curve editor

adding functionality
-------------------------------------------

Autotracks should be able to be overlaid/applied to the sequence
    - target note vel, note timing

select box for PC messages

instead of rand on main menu
    - liveloop?
    - CV?

refactoring/cleaning code
-------------------------------------------

randomize should become an application, replace with CV

bugs (broken features/crashes)
-------------------------------------------
- Recording to Autotracks using joystick/encoders isn't working
- also, the scale selector is too fast
- and the rain app runs too quickly

- tapBPM doesn't work well

- autotrack channel trigger numbers are off by 1

- curve type ++ operator doesn't loop around in curve editor

hmm... autotrack data isn't sent correctly! Pretty bad bug, and weird. Seems to be ableton specific maybe? bascially only like 0--127 are sent, and kind of on time, but no intermediate values.

^^ pretty sure this is only happening when autotracks are sending specific channel+cc values

glitch where trigger autotracks... just play anyway?

NEXT UPDATE:
multiple super position edit at once
layered sequence patterns
switch all usb midi processing to core 0 (swap core1 and core0) and create a fifo system for sending midi messages to core0


NOTES ON MULTICORE MIDI/USB:
https://arduino-pico.readthedocs.io/en/latest/multicore.html
core1 is paused during littleFS operations ^^

TinyUSB is designed to be run from a single core, and it should probably be core0 for maximum compatibility

the stack size is 4K instead of 8K in multicore mode--not sure if this'll be a problem? if it is, you can make 8K stacks for both cores

seems like the fifo buffer isn't gonna work well for sending all the midi data we need to send. instead send a "new midi data to send" flag via fifo and have a volatile midi memory?

do you need to send a note off for all tracks? or will sending the all-off CC value work for most midi devices?
