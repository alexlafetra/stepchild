name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:  # lets you manually trigger a redeploy from the Actions tab

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Emscripten
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: 6.0.5

      - name: Configure
        working-directory: childOS/headless
        run: |
          mkdir -p build-wasm
          cd build-wasm
          emcmake cmake ..

      - name: Build
        working-directory: childOS/headless/build-wasm
        run: emmake make

      - name: Prepare deploy folder
        run: |
          mkdir -p deploy
          cp childOS/headless/build-wasm/headless.html deploy/index.html
          cp childOS/headless/build-wasm/headless.js deploy/
          cp childOS/headless/build-wasm/headless.wasm deploy/
          cp childOS/headless/coi-serviceworker.js deploy/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./deploy